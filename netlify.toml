# Netlify config — เว็บ static ไม่ต้อง build
[build]
  publish = "."
  command = ""

# กันแคชไฟล์ JS/CSS เก่าหลัง deploy ใหม่
[[headers]]
  for = "/js/*"
  [headers.values]
    Cache-Control = "no-cache"
[[headers]]
  for = "/css/*"
  [headers.values]
    Cache-Control = "no-cache"
